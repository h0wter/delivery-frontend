import styled from 'styled-components';
import {
  layout,
  space,
  color,
  border,
  grid,
  flexbox,
  typography,
} from 'styled-system';

export const Box = styled('div')(
  layout,
  space,
  color,
  border,
  grid,
  flexbox,
  typography
);
